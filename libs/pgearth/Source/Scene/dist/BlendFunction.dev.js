"use strict";

define(["../Core/freezeObject", "../Core/WebGLConstants"], function (O, _) {
  "use strict";

  return O({
    ZERO: _.ZERO,
    ONE: _.ONE,
    SOURCE_COLOR: _.SRC_COLOR,
    ONE_MINUS_SOURCE_COLOR: _.ONE_MINUS_SRC_COLOR,
    DESTINATION_COLOR: _.DST_COLOR,
    ONE_MINUS_DESTINATION_COLOR: _.ONE_MINUS_DST_COLOR,
    SOURCE_ALPHA: _.SRC_ALPHA,
    ONE_MINUS_SOURCE_ALPHA: _.ONE_MINUS_SRC_ALPHA,
    DESTINATION_ALPHA: _.DST_ALPHA,
    ONE_MINUS_DESTINATION_ALPHA: _.ONE_MINUS_DST_ALPHA,
    CONSTANT_COLOR: _.CONSTANT_COLOR,
    ONE_MINUS_CONSTANT_COLOR: _.ONE_MINUS_CONSTANT_COLOR,
    CONSTANT_ALPHA: _.CONSTANT_ALPHA,
    ONE_MINUS_CONSTANT_ALPHA: _.ONE_MINUS_CONSTANT_ALPHA,
    SOURCE_ALPHA_SATURATE: _.SRC_ALPHA_SATURATE
  });
});